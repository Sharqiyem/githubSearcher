import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 32,
  },
  resultContainer: {
    marginTop: 16,
    alignItems: 'center',
  },

  flatListContentContainer: {
    paddingBottom: 50,
  },
  separator: {
    height: 32,
  },
});
